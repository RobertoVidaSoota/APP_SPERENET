import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.sperenet',
  appName: 'sperenet',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
